<% json = MagicLamp.generate_all_fixtures.to_json rescue nil %>
<% if json %>
MagicLamp.genie.cacheOnly = true;
MagicLamp.genie.cache = <%= json %>;

<% else %>

throw new Error(MagicLamp.genericError);

<% end %>
