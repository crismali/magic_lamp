
Linting Magic Lamp fixtures...

<% if file_errors.blank? && fixture_errors.blank? %>
Lookin' good!
<% end %>
<% if file_errors.present? %>
The following files could not be loaded:
  <% file_errors.each do |path, error| %>
<%= path %>:
  <%= error %>
  <% end %>
<% end %>
<% if fixture_errors.present? %>
The following fixtures are broken:
  <% fixture_errors.each do |name, fixture_info| %>
"<%= name %>" in <%= fixture_info[:render_block].source_location.first %> on line <%= fixture_info[:render_block].source_location.last %>

  Controller: <%= fixture_info[:controller] %>
  Extensions: <%= fixture_info[:extend].join(", ") %>

  <%= fixture_info[:render_block].source.gsub("\n", "\n  ") %>
  <%= fixture_info[:error] %>
  <% end %>
<% end %>
