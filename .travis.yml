language: ruby
rvm:
  - 2.1.5
  - 2.2.0
before_install:
  - gem update --system
  - gem --version
  - gem install bundler
before_script:
  - ./bootstrap
  - rake -T
cache: bundler
gemfile:
  - gemfiles/rails_4.0.gemfile
  - gemfiles/rails_4.1.gemfile
  - gemfiles/rails_4.2.gemfile
